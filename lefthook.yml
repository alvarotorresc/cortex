pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run --new-from-rev=HEAD {staged_files}
    format:
      glob: "*.go"
      run: gofmt -l -w {staged_files} && git add {staged_files}
